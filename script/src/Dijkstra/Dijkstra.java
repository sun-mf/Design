package Dijkstra;

import java.util.ArrayList;
import Dijkstra.Map.VertexInfo;

public class Dijkstra {
	
	public String findPath(VertexInfo src, VertexInfo target, ArrayList<VertexInfo> vertexs) {
		int[] dis = new int[vertexs.size()];
		for(int i = 0; i < dis.length;i++) {
			
		}
		return "";
	}
}
